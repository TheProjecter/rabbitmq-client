Capable of reading events from a RabbitMQ topic exchange and logs them to console.

Also the code provides a subscriber environment using which custom actions can be written as in when events arrives.